const shirt = {

    color: "blue",
    quantity: 1,
    size: "xl",
    material: "cloth",
    like:  true
}

const shirtSize = shirt.size
const shirtamnt = shirt.quantity
const shirtmaterial = shirt.material
const shirtlike = shirt.like

console.log(shirtSize)
console.log(shirtamnt)
console.log(shirtmaterial)
console.log(shirtlike)